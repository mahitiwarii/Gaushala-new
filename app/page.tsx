'use client'

import { useMemo, useState } from 'react'
import useSWR from 'swr'

const fetcher = (url: string) => fetch(url).then((response) => response.json())
import { ArrowRight, Check, ChevronDown, ChevronLeft, ChevronRight, Heart, Instagram, Leaf, Menu, Minus, PawPrint, Plus, ShoppingBag, Sparkles, X } from 'lucide-react'

const products = [
  { id: 'fodder', title: 'Daily Chara Contribution', detail: 'Green fodder for a healthy, nourishing day.', price: 501, label: 'Donate item', image: '/images/cow-feeding.png' },
  { id: 'feed', title: 'Khal & Nutritious Feed Pack', detail: 'Essential nutrition for growing and recovering cattle.', price: 1100, label: 'Sponsor nutrition', image: '/images/cow-feeding.png' },
  { id: 'aid', title: 'Emergency Medical Aid Kit', detail: 'Immediate treatment for injured and rescued cows.', price: 2100, label: 'Sponsor care', image: '/images/cow-rescue-care.png' },
  { id: 'upla', title: 'Gau Maya Upla', detail: 'Eco-friendly cow dung cakes for home rituals.', price: 299, label: 'Add to cart', image: '/images/cow-dung-products.png' },
  { id: 'dhoop', title: 'Organic Panchagavya & Dhoop', detail: 'Pure, mindful products made with care at the sanctuary.', price: 449, label: 'Add to cart', image: '/images/cow-dung-products.png' },
]

const cows = [
  { name: 'Gauri', age: '8 years', status: 'Recovering well', story: 'Found near a highway with an injured leg. Gauri now walks freely in our orchard.', image: '/images/gir-cows-adoption.png' },
  { name: 'Nandini', age: '12 years', status: 'Gentle senior', story: 'An elderly mother who needed a safe, quiet home and regular joint care.', image: '/images/gir-cows-adoption.png' },
  { name: 'Moti', age: '3 years', status: 'Playful & healthy', story: 'Rescued as a calf, Moti is the first to greet every visiting family.', image: '/images/gir-cows-adoption.png' },
]

const gallery = [
  ['Daily Feeding', '/images/cow-feeding.png'],
  ['Rescue & Care', '/images/cow-rescue-care.png'],
  ['Gau Sewa', '/images/cow-meadow.png'],
  ['Community & Pooja', '/images/cow-pooja-chunni.png'],
  ['Daily Feeding', '/images/cow-feeding.png'],
  ['Gau Sewa', '/images/desi-cow-hero.png'],
]

export default function Page() {
  const { data: cms } = useSWR<{ donations: Array<{ id: string; title: string; description: string; priceInr: number; imageUrl?: string | null }>; cows: Array<{ id: string; name: string; ageAndStatus: string; description: string; imageUrl?: string | null }>; updates: Array<{ id: string; title: string; dateLabel: string; summary: string; imageUrl?: string | null }> }>('/api/content', fetcher)
  const liveProducts = cms?.donations?.length ? cms.donations.map((item) => ({ id: item.id, title: item.title, detail: item.description, price: item.priceInr, label: 'Donate item', image: item.imageUrl || '/images/cow-feeding.png' })) : products
  const liveCows = cms?.cows?.length ? cms.cows.map((item) => ({ name: item.name, age: item.ageAndStatus, status: 'Available for adoption', story: item.description, image: item.imageUrl || '/images/gir-cows-adoption.png' })) : cows
  const liveUpdates = cms?.updates?.length ? cms.updates.map((item) => [item.title, item.summary, item.imageUrl || '/images/cow-rescue-care.png'] as [string, string, string]) : [['July 2026 Rescue Report', '12 new lives found safety this month, and every recovery is possible because of you.', '/images/cow-rescue-care.png'], ['The gift of daily chara', 'How your fodder contributions fed 450+ cows through the summer heat.', '/images/cow-feeding.png'], ['A quiet place to heal', 'Inside our new senior cow meadow, built with a community of caring hands.', '/images/cow-meadow.png']]
  const [menuOpen, setMenuOpen] = useState(false)
  const [cartOpen, setCartOpen] = useState(false)
  const [cart, setCart] = useState<Record<string, number>>({})
  const [selectedDonation, setSelectedDonation] = useState(501)
  const [customAmount, setCustomAmount] = useState('')
  const [galleryFilter, setGalleryFilter] = useState('All')
  const [message, setMessage] = useState('')
  const [paymentOpen, setPaymentOpen] = useState(false)
  const [donationComplete, setDonationComplete] = useState(false)

  const cartItems = liveProducts.filter((product) => cart[product.id])
  const total = useMemo(() => cartItems.reduce((sum, product) => sum + product.price * cart[product.id], 0), [cartItems, cart])
  const cartCount = Object.values(cart).reduce((sum, count) => sum + count, 0)
  const show = (text: string) => { setMessage(text); window.setTimeout(() => setMessage(''), 3000) }
  const addToCart = (id: string) => { setCart((current) => ({ ...current, [id]: (current[id] || 0) + 1 })); setCartOpen(true) }
  const changeQty = (id: string, delta: number) => setCart((current) => ({ ...current, [id]: Math.max(0, (current[id] || 0) + delta) }))
  const shareSeva = async () => {
    const text = 'I donated to Shri Neem Karori Baba Gau Sewa — now it’s your turn to serve a rescued cow.'
    if (navigator.share) await navigator.share({ title: 'Gau Sewa', text, url: window.location.href })
    else window.open(`https://wa.me/?text=${encodeURIComponent(text + ' ' + window.location.href)}`, '_blank', 'noopener,noreferrer')
  }
  const donationAmount = customAmount ? Number(customAmount) : selectedDonation

  return (
    <main className="min-h-screen overflow-hidden bg-background text-foreground">
      {message && <div className="fixed bottom-6 left-1/2 z-[60] -translate-x-1/2 rounded-full bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground shadow-xl" role="status">{message}</div>}
      <header className="sticky top-0 z-40 border-b border-border/60 bg-background/95 backdrop-blur">
        <div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8">
          <a href="#home" className="flex items-center gap-3" aria-label="Shri Neem Karori Baba Gau Sewa home">
            <img src="/images/sanctuary-logo.jpeg" alt="Shri Neem Karori Baba Gau Sewa" className="size-11 rounded-full object-cover" />
            <span className="max-w-44 text-xs font-bold uppercase leading-tight tracking-[0.16em] text-primary sm:max-w-none sm:text-sm">Shri Neem Karori<br className="sm:hidden" /> Baba Gau Sewa</span>
          </a>
          <nav className="hidden items-center gap-6 lg:flex" aria-label="Main navigation">
            {['About Us', 'Donate & Shop', 'Cow Adoption', 'Updates', 'Gallery', 'Contact'].map((item) => <a key={item} href={`#${item.toLowerCase().replaceAll(' ', '-')}`} className="text-sm font-medium text-muted-foreground transition hover:text-primary">{item}</a>)}
          </nav>
          <div className="flex items-center gap-2">
            <button onClick={() => setCartOpen(true)} className="relative grid size-10 place-items-center rounded-full border border-border text-primary transition hover:bg-secondary" aria-label={`Open sewa cart with ${cartCount} items`}><ShoppingBag size={18} />{cartCount > 0 && <span className="absolute -right-1 -top-1 grid size-5 place-items-center rounded-full bg-accent text-[10px] font-bold text-accent-foreground">{cartCount}</span>}</button>
            <a href="#donate-&-shop" className="hidden rounded-full bg-accent px-5 py-2.5 text-sm font-bold text-accent-foreground transition hover:-translate-y-0.5 hover:shadow-lg sm:inline-flex">Donate Now</a>
            <button className="grid size-10 place-items-center rounded-full border border-border lg:hidden" onClick={() => setMenuOpen(!menuOpen)} aria-label="Toggle menu" aria-expanded={menuOpen}>{menuOpen ? <X size={19} /> : <Menu size={19} />}</button>
          </div>
        </div>
        {menuOpen && <nav className="flex flex-col gap-4 border-t border-border/60 px-5 py-5 lg:hidden" aria-label="Mobile navigation">{['About Us', 'Donate & Shop', 'Cow Adoption', 'Updates', 'Gallery', 'Contact'].map((item) => <a key={item} onClick={() => setMenuOpen(false)} href={`#${item.toLowerCase().replaceAll(' ', '-')}`} className="text-sm font-semibold">{item}</a>)}</nav>}
      </header>

      <section id="home" className="relative mx-auto grid max-w-7xl items-center gap-12 px-5 pb-20 pt-14 lg:grid-cols-[1.03fr_.97fr] lg:px-8 lg:pb-28 lg:pt-24">
        <div className="relative z-10"><p className="mb-6 flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] text-accent-foreground"><span className="h-px w-8 bg-accent" /> Seva is a way of life</p><h1 className="max-w-3xl text-balance font-serif text-5xl leading-[1.02] text-primary sm:text-6xl lg:text-7xl">Protect, heal, and <em className="font-serif not-italic text-accent">serve.</em></h1><p className="mt-7 max-w-xl text-pretty text-lg leading-8 text-muted-foreground">Inspired by Neem Karori Baba, we provide immediate medical care, nutritious fodder, and a lifelong home of dignity for abandoned, elderly, and suffering cows.</p><div className="mt-9 flex flex-wrap gap-3"><a href="#donate-&-shop" className="inline-flex items-center gap-2 rounded-full bg-accent px-6 py-3.5 text-sm font-bold text-accent-foreground transition hover:-translate-y-0.5 hover:shadow-lg">Donate Fodder Today <ArrowRight size={16} /></a><a href="#cow-adoption" className="inline-flex items-center gap-2 rounded-full border border-primary/20 px-6 py-3.5 text-sm font-bold text-primary transition hover:border-primary hover:bg-secondary">Explore Cow Adoption</a></div><div className="mt-10 flex items-center gap-3 text-sm text-muted-foreground"><span className="flex -space-x-2">{['https://i.pravatar.cc/80?img=12','https://i.pravatar.cc/80?img=32','https://i.pravatar.cc/80?img=5'].map((src) => <img key={src} src={src} alt="Seva supporter" className="size-8 rounded-full border-2 border-background object-cover" />)}</span><span><strong className="text-primary">2,000+</strong> kind hearts serving with us</span></div></div>
        <div className="relative mx-auto w-full max-w-lg"><div className="absolute -inset-5 rounded-[45%] bg-secondary/70 blur-2xl" /><div className="relative aspect-[.88] overflow-hidden rounded-[48%_48%_18%_18%] bg-muted shadow-2xl"><img src="/images/desi-cow-hero.png" alt="Cow resting peacefully in a green sanctuary" className="h-full w-full object-cover" /></div><div className="absolute -bottom-5 -left-5 flex items-center gap-3 rounded-2xl bg-card p-4 shadow-xl sm:left-0"><span className="grid size-11 place-items-center rounded-full bg-secondary text-primary"><Heart size={20} fill="currentColor" /></span><div><p className="text-xs text-muted-foreground">Every life matters</p><p className="text-sm font-bold text-primary">A home of dignity</p></div></div><div className="absolute -right-2 top-8 grid size-20 place-items-center rounded-full border border-accent/30 bg-background/90 text-center text-[10px] font-bold uppercase leading-tight tracking-widest text-primary shadow-lg sm:-right-6">Om<br />Seva<br />Shanti</div></div>
      </section>

      <section className="bg-primary text-primary-foreground"><div className="mx-auto grid max-w-7xl grid-cols-2 divide-x divide-primary-foreground/15 px-5 py-8 lg:grid-cols-4 lg:px-8">{[['500+', 'Rescued desi cows safe'], ['100%', 'Non-profit transparency'], ['24/7', 'Medical & vet support'], ['Daily', 'Feeding program']].map(([value, label]) => <div key={label} className="px-4 py-3 text-center first:pl-0 last:pr-0 lg:px-8"><p className="font-serif text-3xl text-accent sm:text-4xl">{value}</p><p className="mt-1 text-xs leading-5 text-primary-foreground/70 sm:text-sm">{label}</p></div>)}</div></section>

      <section id="donate-&-shop" className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="mb-10 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-accent-foreground">Choose your seva</p><h2 className="font-serif text-4xl text-primary sm:text-5xl">Small acts, <em className="font-serif not-italic text-accent">lasting care.</em></h2></div><p className="max-w-sm text-sm leading-6 text-muted-foreground">Your contribution becomes fodder, medicine, shelter, and one more peaceful night for a rescued life.</p></div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{liveProducts.map((product, index) => <article key={product.id} className={`group overflow-hidden rounded-3xl border border-border bg-card transition hover:-translate-y-1 hover:shadow-xl ${index === 0 ? 'lg:col-span-2 lg:grid lg:grid-cols-2' : ''}`}><div className={`relative overflow-hidden ${index === 0 ? 'aspect-[1.3] lg:aspect-auto' : 'aspect-[1.45]'}`}><img src={product.image} alt={product.title} className="h-full w-full object-cover transition duration-700 group-hover:scale-105" /><span className="absolute left-4 top-4 rounded-full bg-background/90 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-primary">{index < 3 ? 'Direct seva' : 'Made with care'}</span></div><div className="flex flex-col p-5"><h3 className="font-serif text-2xl text-primary">{product.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{product.detail}</p>{index === 0 && <div className="mt-5 flex flex-wrap gap-2">{[501, 1100, 3100, 5100].map((amount) => <button key={amount} onClick={() => setSelectedDonation(amount)} className={`rounded-full border px-3 py-2 text-xs font-bold transition ${selectedDonation === amount ? 'border-accent bg-accent text-accent-foreground' : 'border-border text-muted-foreground hover:border-accent'}`}>₹{amount.toLocaleString('en-IN')}</button>)}</div>}<label className="mt-4 block text-xs font-semibold text-muted-foreground">Or enter your own amount<input type="number" min="1" value={customAmount} onChange={(event) => setCustomAmount(event.target.value)} placeholder="Custom amount in ₹" className="mt-2 w-full rounded-xl border border-border bg-background px-4 py-3 text-sm text-foreground outline-none focus:ring-2 focus:ring-accent" /></label><div className="mt-auto flex items-center justify-between gap-3 pt-6"><span className="font-serif text-2xl text-primary">₹{index === 0 ? selectedDonation.toLocaleString('en-IN') : product.price.toLocaleString('en-IN')}</span><button onClick={() => addToCart(product.id)} className="inline-flex items-center gap-2 rounded-full bg-primary px-4 py-2.5 text-xs font-bold text-primary-foreground transition hover:bg-primary/90">{product.label} <ArrowRight size={14} /></button></div></div></article>)}</div></section>

      <section id="cow-adoption" className="bg-secondary/60 px-5 py-20 lg:px-8 lg:py-28"><div className="mx-auto max-w-7xl"><div className="grid items-end gap-8 lg:grid-cols-[.8fr_1.2fr]"><div><p className="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-accent-foreground">A relationship, not a transaction</p><h2 className="font-serif text-4xl text-primary sm:text-5xl">Meet the hearts<br /><em className="font-serif not-italic text-accent">you can hold close.</em></h2></div><p className="max-w-lg text-sm leading-7 text-muted-foreground">Adoption is a monthly promise of food, medical care, and love. Choose a resident and become part of their recovery story.</p></div><div className="mt-10 grid gap-6 md:grid-cols-3">{liveCows.map((cow) => <article key={cow.name} className="overflow-hidden rounded-3xl bg-card shadow-sm"><div className="aspect-[1.25] overflow-hidden"><img src={cow.image} alt={`${cow.name}, a sanctuary cow`} className="h-full w-full object-cover transition duration-500 hover:scale-105" /></div><div className="p-5"><div className="flex items-start justify-between"><div><h3 className="font-serif text-2xl text-primary">{cow.name}</h3><p className="text-xs text-muted-foreground">{cow.age} · {cow.status}</p></div><span className="grid size-9 place-items-center rounded-full bg-secondary text-primary"><Heart size={16} /></span></div><p className="mt-4 text-sm leading-6 text-muted-foreground">{cow.story}</p><button onClick={() => show(`Thank you for choosing to sponsor ${cow.name}.`)} className="mt-5 w-full rounded-full border border-primary/20 py-3 text-xs font-bold text-primary transition hover:border-primary hover:bg-secondary">Adopt {cow.name} monthly</button></div></article>)}</div></div></section>

      <section id="updates" className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="flex items-end justify-between"><div><p className="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-accent-foreground">Open books, open hearts</p><h2 className="font-serif text-4xl text-primary sm:text-5xl">From the <em className="font-serif not-italic text-accent">sanctuary.</em></h2></div><a href="#contact" className="hidden items-center gap-2 text-sm font-bold text-primary sm:flex">View all updates <ArrowRight size={16} /></a></div><div className="mt-10 grid gap-6 md:grid-cols-3">{liveUpdates.map(([title, detail, image]) => <article key={title} className="group"><div className="aspect-[1.7] overflow-hidden rounded-2xl"><img src={image} alt="Sanctuary update" className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /></div><p className="mt-5 text-xs font-bold uppercase tracking-widest text-accent-foreground">Monthly update · 2026</p><h3 className="mt-2 font-serif text-2xl text-primary">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{detail}</p><button onClick={() => show('This story is coming soon.')} className="mt-4 inline-flex items-center gap-2 text-xs font-bold text-primary">Read story <ArrowRight size={14} /></button></article>)}</div></section>

      <section id="reviews" className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-24"><div className="mb-10"><p className="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-accent-foreground">Words from kind hearts</p><h2 className="font-serif text-4xl text-primary sm:text-5xl">Seva shared, <em className="font-serif not-italic text-accent">love returned.</em></h2></div><div className="grid gap-5 md:grid-cols-3">{[['Ananya Sharma', 'The transparency and daily updates gave me complete confidence. Supporting a cow feels deeply meaningful.', 'Monthly donor'], ['Rahul Mehta', 'The team treats every rescued mother with so much patience and dignity. Jai Gau Mata.', 'Cow sponsor'], ['Priya Kapoor', 'I visited the gaushala and saw the care firsthand. It is a peaceful, sincere place of service.', 'Community visitor']].map(([name, quote, role]) => <article key={name} className="rounded-3xl border border-border bg-card p-6"><div className="flex gap-1 text-accent" aria-label="5 out of 5 stars">★★★★★</div><p className="mt-4 text-sm leading-7 text-muted-foreground">“{quote}”</p><div className="mt-6 border-t border-border pt-4"><p className="font-bold text-primary">{name}</p><p className="text-xs text-muted-foreground">{role}</p></div></article>)}</div><button onClick={() => show('Thank you for your interest. Review submission will open soon.')} className="mt-7 rounded-full border border-primary/20 px-5 py-3 text-sm font-bold text-primary hover:bg-secondary">Share your review</button></section>

      <section id="gallery" className="bg-primary px-5 py-20 text-primary-foreground lg:px-8 lg:py-28"><div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><p className="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-accent">Life at Gau Sewa</p><h2 className="font-serif text-4xl sm:text-5xl">Gau Sewa in <em className="font-serif not-italic text-accent">motion.</em></h2></div><div className="flex flex-wrap gap-2">{['All', 'Daily Feeding', 'Rescue & Care', 'Gau Sewa'].map((filter) => <button key={filter} onClick={() => setGalleryFilter(filter)} className={`rounded-full border px-3 py-2 text-xs transition ${galleryFilter === filter ? 'border-accent bg-accent text-accent-foreground' : 'border-primary-foreground/20 text-primary-foreground/70 hover:border-accent'}`}>{filter}</button>)}</div></div><div className="mt-10 grid grid-cols-2 gap-3 md:grid-cols-3">{gallery.filter(([category]) => galleryFilter === 'All' || category === galleryFilter).map(([category, image], index) => <div key={`${category}-${index}`} className={`group relative overflow-hidden rounded-2xl ${index === 0 ? 'col-span-2 aspect-[2/1] md:col-span-2' : 'aspect-square'}`}><img src={image} alt={category} className="h-full w-full object-cover transition duration-700 group-hover:scale-105" /><span className="absolute bottom-3 left-3 rounded-full bg-primary/80 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-primary-foreground">{category}</span></div>)}</div></div></section>

      <section id="contact" className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-24"><div className="grid gap-10 rounded-[2rem] bg-accent p-8 sm:p-12 lg:grid-cols-[1fr_.8fr] lg:p-16"><div><p className="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-accent-foreground/70">Stay connected to seva</p><h2 className="max-w-xl font-serif text-4xl text-accent-foreground sm:text-5xl">Come closer to the work your kindness makes possible.</h2><p className="mt-5 max-w-md text-sm leading-7 text-accent-foreground/75">Get our monthly rescue notes, transparent reports, and simple ways to serve from wherever you are.</p></div><form onSubmit={(event) => { event.preventDefault(); show('Thank you. You are now part of our monthly seva notes.'); }} className="self-end"><label htmlFor="email" className="sr-only">Email address</label><div className="flex flex-col gap-3 sm:flex-row"><input id="email" type="email" required placeholder="Your email address" className="min-w-0 flex-1 rounded-full border-0 bg-background px-5 py-3.5 text-sm text-foreground outline-none placeholder:text-muted-foreground focus:ring-2 focus:ring-primary" /><button className="rounded-full bg-primary px-6 py-3.5 text-sm font-bold text-primary-foreground transition hover:bg-primary/90">Sign me up</button></div><p className="mt-3 text-xs text-accent-foreground/60">No noise. Just meaningful updates from the sanctuary.</p></form></div></section>

      <footer className="border-t border-border bg-background px-5 py-12 lg:px-8"><div className="mx-auto grid max-w-7xl gap-10 md:grid-cols-[1.4fr_1fr_1fr_1fr]"><div><div className="flex items-center gap-3"><img src="/images/sanctuary-logo.jpeg" alt="Shri Neem Karori Baba Gau Sewa" className="size-10 rounded-full object-cover" /><span className="text-xs font-bold uppercase tracking-[0.14em] text-primary">Shri Neem Karori<br />Baba Gau Sewa</span></div><p className="mt-5 max-w-xs text-sm leading-6 text-muted-foreground">A compassionate sanctuary for abandoned, injured, and elderly cattle in Noida / NCR.</p><p className="mt-4 text-sm font-semibold text-primary">Open daily, 24/7 · Emergency rescue: 9811319139</p><div className="mt-5 flex items-center gap-2"><a href="https://wa.me/919811319139" target="_blank" rel="noreferrer" aria-label="Connect with us on WhatsApp" title="WhatsApp" className="grid size-10 place-items-center rounded-full bg-secondary transition hover:-translate-y-0.5 hover:bg-secondary/70"><img src="https://cdn.jsdelivr.net/gh/glincker/thesvg@main/public/icons/whatsapp/default.svg" alt="" className="size-5" /></a><a href="https://www.instagram.com/neemkaroribabagausewa?igsi=a3RrdXV4MDQ5YTdr" target="_blank" rel="noreferrer" aria-label="Follow us on Instagram" title="Instagram" className="grid size-10 place-items-center rounded-full border border-border transition hover:-translate-y-0.5 hover:bg-secondary"><img src="https://cdn.jsdelivr.net/gh/glincker/thesvg@main/public/icons/instagram/default.svg" alt="" className="size-5" /></a></div></div><div><h3 className="text-xs font-bold uppercase tracking-widest text-primary">Visit & contact</h3><p className="mt-4 text-sm leading-7 text-muted-foreground">Noida / NCR Sanctuary Location<br />Open daily, 24 hours · 7 days a week<br /><a href="mailto:neemkaroribabagausewa@gmail.com" className="hover:text-primary">neemkaroribabagausewa@gmail.com</a><br /><a href="tel:9811319139" className="hover:text-primary">9811319139</a></p></div><div><h3 className="text-xs font-bold uppercase tracking-widest text-primary">Explore</h3><div className="mt-4 flex flex-col gap-3 text-sm text-muted-foreground"><a href="#about-us" className="hover:text-primary">Our story</a><a href="#donate-&-shop" className="hover:text-primary">Donate & shop</a><a href="#cow-adoption" className="hover:text-primary">Adopt a cow</a><a href="#updates" className="hover:text-primary">Transparency reports</a></div></div><div><h3 className="text-xs font-bold uppercase tracking-widest text-primary">Trust & seva</h3><p className="mt-4 text-sm leading-6 text-muted-foreground">Donations are eligible for tax exemption under applicable Indian laws. Receipts are shared by email.</p><div className="mt-5 flex gap-2"><span className="grid size-9 place-items-center rounded-full border border-border text-xs font-bold text-primary">ig</span><span className="grid size-9 place-items-center rounded-full border border-border text-xs font-bold text-primary">f</span><span className="grid size-9 place-items-center rounded-full border border-border text-xs font-bold text-primary">yt</span></div></div></div><div className="mx-auto mt-10 max-w-7xl border-t border-border pt-6 text-xs text-muted-foreground">© 2026 Shri Neem Karori Baba Gau Sewa. Made for seva.</div></footer>

      {paymentOpen && <div className="fixed inset-0 z-[70] grid place-items-center bg-primary/50 p-4" onClick={() => setPaymentOpen(false)}><section role="dialog" aria-modal="true" aria-labelledby="payment-title" className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-3xl bg-background p-6 shadow-2xl sm:p-8" onClick={(event) => event.stopPropagation()}>{donationComplete ? <div className="grid min-h-80 place-items-center text-center"><div><span className="mx-auto grid size-16 place-items-center rounded-full bg-secondary text-primary"><Check size={30} /></span><h2 id="payment-title" className="mt-5 font-serif text-4xl text-primary">Thank you for donating.</h2><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-muted-foreground">Your seva supports safe shelter, nutritious fodder, and loving care for Indian desi cows.</p><button onClick={shareSeva} className="mt-7 inline-flex items-center gap-2 rounded-full bg-accent px-6 py-3 text-sm font-bold text-accent-foreground">I donated — now it’s your turn <ArrowRight size={15} /></button><button onClick={() => setPaymentOpen(false)} className="ml-2 mt-7 rounded-full border border-border px-6 py-3 text-sm font-bold text-primary">Close</button></div></div> : <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-accent-foreground">Complete your seva</p><h2 id="payment-title" className="mt-1 font-serif text-3xl text-primary">Donate securely</h2><p className="mt-2 text-sm text-muted-foreground">Total contribution: <strong className="text-primary">₹{total.toLocaleString('en-IN')}</strong></p></div><button onClick={() => setPaymentOpen(false)} className="grid size-10 place-items-center rounded-full border border-border" aria-label="Close payment dialog"><X size={18} /></button></div><div className="mt-6 grid gap-6 md:grid-cols-[220px_1fr]"><div className="rounded-2xl border border-border bg-card p-4 text-center"><img src="/images/payment-qr.jpeg" alt="Scan QR code to donate" className="mx-auto aspect-square w-full rounded-lg object-cover" /><p className="mt-3 text-xs font-semibold text-primary">Scan to donate via UPI</p></div><div className="rounded-2xl bg-secondary/60 p-5"><h3 className="font-serif text-2xl text-primary">Bank details</h3><dl className="mt-4 space-y-3 text-sm"><div className="flex justify-between gap-4 border-b border-border/60 pb-2"><dt className="text-muted-foreground">Company</dt><dd className="text-right font-bold text-primary">SAMMAAN FOUNDATION</dd></div><div className="flex justify-between gap-4 border-b border-border/60 pb-2"><dt className="text-muted-foreground">Account number</dt><dd className="text-right font-bold text-primary">10245035190</dd></div><div className="flex justify-between gap-4 border-b border-border/60 pb-2"><dt className="text-muted-foreground">IFSC</dt><dd className="text-right font-bold text-primary">IDFB0021331</dd></div><div className="flex justify-between gap-4 border-b border-border/60 pb-2"><dt className="text-muted-foreground">SWIFT</dt><dd className="text-right font-bold text-primary">IDFBINBBMUM</dd></div><div className="flex justify-between gap-4 border-b border-border/60 pb-2"><dt className="text-muted-foreground">Bank</dt><dd className="text-right font-bold text-primary">IDFC FIRST</dd></div><div className="flex justify-between gap-4"><dt className="text-muted-foreground">Branch</dt><dd className="text-right font-bold text-primary">Noida Sector 63 Branch</dd></div></dl></div></div><button onClick={() => setDonationComplete(true)} className="mt-6 w-full rounded-full bg-accent py-4 text-sm font-bold text-accent-foreground">I have completed my donation</button></>}</section></div>}

      {cartOpen && <div className="fixed inset-0 z-50 bg-primary/30" onClick={() => setCartOpen(false)}><aside className="ml-auto flex h-full w-full max-w-md flex-col bg-background p-6 shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex items-center justify-between border-b border-border pb-5"><div><p className="text-xs font-bold uppercase tracking-widest text-accent-foreground">Your seva cart</p><h2 className="mt-1 font-serif text-3xl text-primary">Ready to make impact?</h2></div><button onClick={() => setCartOpen(false)} className="grid size-10 place-items-center rounded-full border border-border" aria-label="Close cart"><X size={18} /></button></div><div className="flex-1 py-6">{cartItems.map((product) => <div key={product.id} className="flex gap-3"><img src={product.image} alt="" className="size-16 rounded-xl object-cover" /><div className="min-w-0 flex-1"><h3 className="font-bold text-primary">{product.title}</h3><p className="mt-1 text-sm text-muted-foreground">₹{product.price.toLocaleString("en-IN")}</p><div className="mt-2 flex items-center gap-3"><button onClick={() => changeQty(product.id, -1)} className="grid size-7 place-items-center rounded-full border border-border" aria-label="Decrease quantity"><Minus size={13} /></button><span className="text-sm">{cart[product.id]}</span><button onClick={() => changeQty(product.id, 1)} className="grid size-7 place-items-center rounded-full border border-border" aria-label="Increase quantity"><Plus size={13} /></button></div></div><p className="font-bold text-primary">₹{(product.price * cart[product.id]).toLocaleString("en-IN")}</p></div>)}</div><div className="border-t border-border pt-5"><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">Total seva</span><strong className="text-xl text-primary">₹{total.toLocaleString("en-IN")}</strong></div><button onClick={() => { setCartOpen(false); setDonationComplete(false); setSelectedDonation(donationAmount); setPaymentOpen(true) }} className="mt-6 flex w-full items-center justify-center gap-2 rounded-full bg-accent py-4 text-sm font-bold text-accent-foreground">Continue with Sewa <ArrowRight size={16} /></button></div></aside></div>}
    </main>
  )
}
